#include "holberton.h"

/**
 * mul - multiplies the two entered integers
 *@a: the first integer to be multiplied
 *@b: the second integer to be multiplied
 *
 * Return: the product f the two integers
 */
int mul(int a, int b)
{
	int Santana = (a * b);

	return (Santana);
}
