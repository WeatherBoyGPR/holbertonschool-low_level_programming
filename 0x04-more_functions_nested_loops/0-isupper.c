#include "holberton.h"

/**
 * _isupper - determines if c is uppercase
 * @c: is the integer that is operated on
 *
 * Return: afdafsdf.
 */
int _isupper(int c)
{
	int magentamagenta;

	magentamagenta = (0);
	if (c >= 65 && c <= 90)
	{
		magentamagenta = (1);
	}
	return (magentamagenta);
}
